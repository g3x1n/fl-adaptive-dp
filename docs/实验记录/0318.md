我们已经把“先只调 `FedAvg + IID` 参数”这件事往前推进了一步，不过我顺手把第 1 组基线实验完整跑出来了，这样你不会只拿到一堆零散结果。

我参考了 `FedAvg` 原文 [Communication-Efficient Learning of Deep Networks from Decentralized Data](https://huggingface.co/papers/1602.05629)，以及 `FedProx` 官方仓库对 `mu` 调参的建议 [litian96/FedProx](https://github.com/litian96/FedProx)，先选了一套“贴近论文思路、但适合当前机器规模”的参数：
- `MNIST`
- `num_clients = 10`
- `fraction_fit = 0.5`
- `rounds = 10`
- `local_epochs = 1`
- `batch_size = 64`
- `learning_rate = 0.01`
- `alpha = 0.1` 作为强 Non-IID
- `FedProx mu = 0.01`

我已经把这 6 组实验都跑完了，结果和分析写进了：
[实验1-基线算法对比实验记录.md](/Users/admin/Desktop/fl-adaptive-dp/docs/实验1-基线算法对比实验记录.md)

这次最关键的结果是：
- `FedAvg IID`: `79.95%`
- `FedAvg Non-IID`: `50.20%`
- `FedProx IID`: `79.90%`
- `FedProx Non-IID`: `50.20%`
- `FedNova IID`: `79.95%`
- `FedNova Non-IID`: `48.70%`

能明确得到的结论是：
- 强 Non-IID 会显著拉低性能，这一点已经被清楚验证了。
- IID 下三种算法几乎一致。
- 但你说得对，`79.9%` 的 IID 精度本身偏低，所以这还不是最终论文该用的“定稿参数”。
- 目前这组更适合当第一轮摸底实验，而不是最终结论实验。

所以我很认同你现在的判断：下一步应该真的只做一件事，就是**先调 `FedAvg + IID` 公共参数**，把基础工作点抬高。  
我建议我们下一轮直接做一个小型调参网格，先试：
- `rounds = 20, 30`
- `learning_rate = 0.01, 0.02, 0.05`
- `fraction_fit = 0.5, 1.0`

这 12 组最值钱，足够把 `FedAvg IID` 调到一个更像样的水平。  
如果你点头，我下一步就直接帮你生成这 12 个配置并开始跑。